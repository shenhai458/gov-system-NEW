# Neon / Vercel Postgres connection string (e.g. from Vercel Marketplace - Neon)
POSTGRES_URL=postgresql://user:password@localhost:5432/gov_projects

# JWT secret (generate a strong random string for production)
APP_SECRET=your-secret-key-here

# Optional: Application ID (legacy, kept for compatibility)
APP_ID=gov-project-management
