# 政府项目申报管理系统 —— 改造后技术文档

> 文档版本：v2.0  
> 编写时间：2026-08-02  
> 对应代码目录：`/d/workbuddy/2026-08-02-10-41-25/transformed`

---

## 一、概述

本次改造以“最大化适配 Vercel Serverless 平台”为核心目标，在保留原系统功能完整性的基础上，完成了以下关键升级：

1. **数据库迁移**：由 MySQL 迁移至 **Neon Postgres**（Vercel Marketplace 原生集成）。
2. **后端架构调整**：移除 `@hono/node-server`，改用 Hono + tRPC 的 **Fetch API 适配层**，通过 `api/trpc.ts` 导出 Vercel Serverless Function。
3. **权限模型增强**：新增 `adminProcedure`，所有写操作仅限 `role === "admin"` 的用户执行，普通用户仅可查看。
4. **可配置化能力**：
   - 仪表盘统计卡片支持管理员增删改（`dashboard_widgets`）。
   - 数据分析顶部卡片支持管理员增删改（`analytics_cards`）。
   - 项目管理字段支持自定义增删改（`project_fields` + `projects.customData`）。
5. **数据勾稽联动**：所有统计、图表、卡片均基于同一套项目与荣誉数据源实时计算，避免数据孤岛。
6. **新增模块**：企业荣誉库（`honors` + `honor_renewals`），支持荣誉资质管理与复审周期跟踪。
7. **构建验证**：已通过 `npm run check`（TypeScript）与 `npm run build`（Vite 生产构建）。

---

## 二、技术栈

| 层级 | 技术 |
|------|------|
| 前端框架 | React 19 + TypeScript |
| 构建工具 | Vite 7 |
| 样式 | Tailwind CSS 3 + shadcn/ui |
| 图表 | Recharts |
| 路由 | React Router 7 |
| 后端框架 | Hono 4 |
| API 协议 | tRPC 11 |
| ORM | Drizzle ORM 0.45 |
| 数据库 | Neon Postgres（`@neondatabase/serverless`） |
| 部署平台 | Vercel |
| 鉴权 | JWT（jose） |

---

## 三、项目目录结构

```
transformed/
├── api/                          # 后端入口与业务路由
│   ├── app.ts                    # Hono 应用，挂载 tRPC Fetch handler
│   ├── trpc.ts                   # Vercel Serverless Function 入口
│   ├── context.ts                # tRPC 上下文（解析 JWT）
│   ├── middleware.ts             # public / authed / admin Procedure
│   ├── router.ts                 # tRPC 路由聚合
│   ├── lib/
│   │   ├── env.ts                # 环境变量读取
│   │   ├── auth.ts               # JWT 签发与校验
│   │   └── stats.ts              # 项目统计计算工具
│   ├── queries/
│   │   └── connection.ts         # Neon HTTP 数据库连接
│   └── routers/
│       ├── auth.ts               # 登录/登出/当前用户
│       ├── project.ts            # 项目 CRUD + 自定义字段管理
│       ├── dashboard.ts          # 仪表盘卡片 + 统计数据
│       ├── analytics.ts          # 数据分析图表 + 分析卡片
│       ├── honor.ts              # 企业荣誉库 + 复审记录
│       ├── user.ts               # 用户管理
│       └── setting.ts            # 系统设置
├── db/
│   ├── schema.ts                 # Drizzle PostgreSQL 表结构
│   └── relations.ts              # 表关系定义
├── src/
│   ├── App.tsx                   # 路由配置
│   ├── providers/trpc.tsx        # tRPC + React Query 客户端
│   ├── hooks/useAuth.ts          # 当前用户与管理员判断
│   ├── components/Layout.tsx     # 侧边栏布局
│   ├── components/ui/            # shadcn/ui 组件库
│   └── pages/                    # 页面组件
│       ├── Dashboard.tsx
│       ├── Projects.tsx
│       ├── Analytics.tsx
│       ├── Honors.tsx
│       ├── Users.tsx
│       ├── Settings.tsx
│       └── Login.tsx
├── docs/
│   ├── 01-改造前技术文档.md
│   └── 02-改造后技术文档.md      # 本文档
├── dist/                         # Vite 生产构建输出
├── package.json
├── vite.config.ts
├── vercel.json
├── drizzle.config.ts
└── README.md
```

---

## 四、数据库设计（Neon Postgres）

### 4.1 表清单

| 表名 | 说明 | 核心字段 |
|------|------|----------|
| `users` | 用户表 | `username`, `password`, `real_name`, `role`, `status` |
| `projects` | 项目表 | 基础字段 + `custom_data jsonb` 存储动态字段 |
| `project_fields` | 项目自定义字段元数据 | `key`, `label`, `field_type`, `options`, `order`, `is_visible` |
| `settings` | 系统设置 | 通知、安全、备份配置 |
| `dashboard_widgets` | 仪表盘统计卡片 | `title`, `type`, `icon`, `color`, `order`, `is_visible` |
| `analytics_cards` | 数据分析卡片 | 同仪表盘卡片模型 |
| `honors` | 企业荣誉/资质 | `title`, `type`, `level`, `issue_date`, `expiry_date`, `review_cycle_months`, `status`, `project_id` |
| `honor_renewals` | 荣誉复审记录 | `honor_id`, `planned_date`, `actual_date`, `status`, `cost` |

### 4.2 枚举类型

```ts
role: ["admin", "user", "visitor"]
user_status: ["active", "disabled"]
project_status: ["ok", "failed", "in_progress", "pending"]
warning: ["normal", "7days", "15days", "30days"]
field_type: ["text", "number", "date", "select", "textarea"]
widget_type: ["total", "inProgress", "completed", "failed", "pending", "totalAmount", "successRate", "activeProjects"]
honor_type: ["honor", "qualification", "certification", "other"]
honor_level: ["national", "provincial", "city", "district", "other"]
honor_status: ["valid", "expiring", "expired"]
renewal_status: ["planned", "in_progress", "completed", "overdue"]
```

### 4.3 关键设计说明

- **自定义字段**：`project_fields` 定义字段的元数据，`projects.customData` 以 JSONB 形式存储每个项目的动态字段值，实现字段的灵活扩展。
- **可配置卡片**：`dashboard_widgets` 与 `analytics_cards` 让管理员可自由增删改首页与分析页卡片。
- **荣誉状态自动计算**：`honors.status` 在查询时根据 `expiryDate` 动态计算（有效 / 即将到期 90 天内 / 已过期），无需人工维护。
- **数据勾稽**：`honors.project_id` 关联 `projects.id`，实现项目成果与荣誉资质的联动。

---

## 五、后端架构

### 5.1 Vercel Serverless 入口

```ts
// api/trpc.ts
import { handle } from "hono/vercel";
import app from "./app";
export default handle(app);
```

```ts
// api/app.ts
import { Hono } from "hono";
import { fetchRequestHandler } from "@trpc/server/adapters/fetch";

const app = new Hono();
app.use("/api/trpc/*", async (c) => {
  return fetchRequestHandler({
    endpoint: "/api/trpc",
    req: c.req.raw,
    router: appRouter,
    createContext,
  });
});
```

### 5.2 tRPC 上下文与权限

```ts
// api/middleware.ts
export const publicProcedure = t.procedure;

export const authedProcedure = t.procedure.use(({ ctx, next }) => {
  if (!ctx.user) throw new TRPCError({ code: "UNAUTHORIZED" });
  return next({ ctx: { ...ctx, user: ctx.user } });
});

export const adminProcedure = t.procedure.use(({ ctx, next }) => {
  if (!ctx.user) throw new TRPCError({ code: "UNAUTHORIZED" });
  if (ctx.user.role !== "admin") throw new TRPCError({ code: "FORBIDDEN" });
  return next({ ctx: { ...ctx, user: ctx.user } });
});
```

- 所有 `list`、`getById`、`stats` 等读取接口使用 `publicProcedure`（仍要求登录，由上下文 JWT 校验）。
- 所有 `create`、`update`、`delete` 写接口使用 `adminProcedure`，确保非管理员无法修改数据。

### 5.3 数据库连接（Neon HTTP）

```ts
// api/queries/connection.ts
import { drizzle } from "drizzle-orm/neon-http";
import { neon } from "@neondatabase/serverless";

const sql = neon(env.postgresUrl);
const db = drizzle(sql, { schema: fullSchema });
```

使用 Neon 的 HTTP 驱动，天然适配 Vercel Serverless 的无连接池环境，避免传统 TCP 连接在 Serverless 中的冷启动问题。

---

## 六、核心功能实现

### 6.1 仪表盘（Dashboard）

- 卡片从 `dashboard_widgets` 动态渲染。
- 管理员可新增、编辑、删除、调整顺序、显示/隐藏卡片。
- 每种 `type` 对应一种统计指标，由统一的 `computeProjectStats` 计算。
- 非管理员仅展示数据，隐藏所有操作按钮。

### 6.2 项目管理（Projects）

- 表格列与新增/编辑表单根据 `project_fields` 动态生成。
- 支持字段类型：文本、数字、日期、下拉选择、多行文本。
- 管理员可在页面内打开“字段配置”弹窗，增删改字段。
- 删除字段不会影响历史项目数据（数据保留在 `customData` 中）。

### 6.3 数据分析（Analytics）

- 6 种图表保持原功能：申报趋势、部门分布、状态分析、申报人绩效、月度金额、部门成功率。
- 顶部关键指标卡片从 `analytics_cards` 动态渲染，管理员可配置。
- 所有指标均读取 `projects` 表实时计算。

### 6.4 企业荣誉库（Honors）

- 荣誉/资质 CRUD。
- 支持设置复审周期（月）。
- 根据到期日自动计算状态：有效、即将到期、已过期。
- 每条荣誉可维护多条复审记录 `honor_renewals`。
- 删除荣誉时级联删除其复审记录。

### 6.5 用户管理与系统设置

- 用户管理仅管理员可操作，普通用户只读。
- 系统设置页面非管理员隐藏保存按钮。

---

## 七、权限控制

| 角色 | 读权限 | 写权限 |
|------|--------|--------|
| admin | 全部 | 全部 |
| user / visitor | 全部 | 无 |

实现方式：
- 后端：tRPC `adminProcedure` 拦截所有写操作。
- 前端：`useAuth` 暴露 `isAdmin`，页面根据该状态隐藏/禁用操作按钮。

---

## 八、Vercel 部署配置

```json
// vercel.json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/trpc/:path*", "destination": "/api/trpc" }
  ],
  "headers": [
    {
      "source": "/api/trpc/:path*",
      "headers": [
        { "key": "Access-Control-Allow-Origin", "value": "*" },
        { "key": "Access-Control-Allow-Methods", "value": "GET, POST, OPTIONS" },
        { "key": "Access-Control-Allow-Headers", "value": "Content-Type, Authorization" }
      ]
    }
  ]
}
```

### 部署步骤

1. 在 Vercel 创建项目，导入本仓库。
2. 在 Vercel 控制台添加环境变量：
   - `POSTGRES_URL`：Neon Postgres 连接串（推荐在 Vercel Marketplace 集成 Neon，自动注入）。
   - `APP_SECRET`：JWT 签名密钥（长度建议 ≥ 32 字节）。
3. 执行 `npm run db:push` 推送数据库结构（本地或在 Vercel Build Command 中执行）。
4. （可选）执行 `npm run db:seed` 初始化管理员账号与默认数据。
5. Vercel 自动构建并部署前端 `dist/` 与后端 `/api/trpc`。

---

## 九、环境变量

| 变量名 | 必填 | 说明 |
|--------|------|------|
| `POSTGRES_URL` / `DATABASE_URL` | 是 | Neon Postgres 连接串 |
| `APP_SECRET` / `APP_SECRET_KEY` | 是 | JWT 签名密钥 |
| `NODE_ENV` | 否 | production 时启用严格校验 |

---

## 十、脚本说明

```json
{
  "dev": "vite",
  "build": "vite build",
  "check": "tsc -b",
  "db:generate": "drizzle-kit generate",
  "db:migrate": "drizzle-kit migrate",
  "db:push": "drizzle-kit push",
  "db:seed": "tsx db/seed.ts"
}
```

---

## 十一、改造前后对比

| 维度 | 改造前 | 改造后 |
|------|--------|--------|
| 数据库 | MySQL（需长期 TCP 连接） | Neon Postgres（HTTP 驱动，Serverless 友好） |
| 后端运行 | Node Server (`@hono/node-server`) | Vercel Serverless Function (`hono/vercel`) |
| 部署平台 | 通用 Node 服务器 | Vercel 原生适配 |
| 仪表盘卡片 | 硬编码 | 数据库驱动，可配置 |
| 分析卡片 | 硬编码 | 数据库驱动，可配置 |
| 项目字段 | 固定表结构 | JSONB 动态扩展 |
| 权限 | 仅登录校验 | 登录 + 管理员角色校验 |
| 荣誉库 | 无 | 新增完整模块 |
| 数据联动 | 较弱 | 统一统计源，实时联动 |

---

## 十二、构建验证结果

```bash
npm run check  # TypeScript 类型检查通过
npm run build  # Vite 生产构建成功，输出 dist/
```

构建产物：

```
dist/index.html
 dist/assets/index-B3WPLVUr.css   90.14 kB │ gzip: 14.75 kB
 dist/assets/index-BEM2KR9T.js   908.03 kB │ gzip: 243.28 kB
```

---

## 十三、后续建议

1. **数据库迁移**：首次部署前，使用 `drizzle-kit push` 将表结构推送到 Neon；如需迁移旧 MySQL 数据，可编写一次性 ETL 脚本。
2. **构建产物优化**：当前 JS 包约 908KB，建议通过 `React.lazy` 对页面进行代码分割，降低首屏加载。
3. **安全加固**：生产环境务必设置强 `APP_SECRET`，并启用 Vercel 的 HTTPS 强制跳转。
4. **备份策略**：利用 Neon 的自动备份功能，替代原系统的本地备份配置。
5. **日志与监控**：可接入 Vercel Analytics 与 Neon 监控面板观察性能。

---

*文档结束。*
