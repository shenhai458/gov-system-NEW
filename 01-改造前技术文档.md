# 政府项目申报管理系统 — 改造前技术文档

> 版本：v1.0  
> 编写时间：2026-08-02  
> 对应代码：`original/app`

---

## 1. 项目概述

本项目是一套面向政府项目申报场景的管理系统，支持项目全生命周期管理、数据分析、用户权限与系统配置。原系统由 Kimi Agent 生成，采用前后端一体化（Full-Stack）架构，开发阶段由 Vite 同时承载前端 HMR 与后端 Hono 服务。

### 1.1 主要功能模块

| 模块 | 说明 |
|------|------|
| 登录认证 | 用户名/密码登录、JWT Token 认证、退出登录 |
| 仪表板 | 统计卡片、项目状态饼图、月度趋势面积图、最近添加、即将到期提醒、快速统计、项目列表 |
| 项目管理 | 完整 CRUD、多条件筛选、分页、搜索 |
| 数据分析 | 申报趋势、部门分布、状态分析、申报人绩效、月度金额、部门成功率 6 种图表 |
| 用户管理 | 用户增删改查、启用/禁用账户 |
| 系统设置 | 基本设置、通知设置、安全设置、数据备份配置 |

---

## 2. 技术架构

### 2.1 前端技术栈

- **框架**：React 19（函数组件 + Hooks）
- **语言**：TypeScript 5.9
- **构建工具**：Vite 7.2
- **样式**：Tailwind CSS 3.4
- **组件库**：shadcn/ui（Radix UI + Tailwind）
- **图表库**：Recharts 2.15
- **路由**：React Router 7
- **状态/请求**：tRPC + React Query（@tanstack/react-query）
- **表单**：React Hook Form + Zod

### 2.2 后端技术栈

- **Web 框架**：Hono 4.8
- **API 协议**：tRPC 11（HTTP Batch Link + SuperJSON）
- **ORM**：Drizzle ORM 0.45
- **数据库**：MySQL（通过 `mysql2` 驱动，PlanetScale 兼容模式）
- **认证**：Jose（HS256 JWT）
- **密码加密**：bcryptjs

---

## 3. 目录结构

```
app/
├── api/                          # 后端入口与路由
│   ├── boot.ts                   # 生产环境启动入口（Node.js + @hono/node-server）
│   ├── context.ts                # tRPC 上下文（解析 JWT）
│   ├── middleware.ts             # tRPC 中间件（public / authed / admin）
│   ├── router.ts                 # tRPC 根路由聚合
│   ├── lib/                      # 工具库
│   │   ├── auth.ts               # JWT 签发/校验
│   │   ├── env.ts                # 环境变量读取
│   │   ├── http.ts               # HTTP 工具
│   │   └── vite.ts               # 生产环境静态文件服务
│   ├── queries/connection.ts     # Drizzle + MySQL 连接
│   └── routers/                  # 业务路由
│       ├── auth.ts               # 登录、获取当前用户
│       ├── project.ts            # 项目 CRUD + 统计
│       ├── user.ts               # 用户 CRUD
│       ├── setting.ts            # 系统设置
│       └── analytics.ts          # 数据分析
├── contracts/                    # 前后端共享类型/错误
├── db/                           # 数据库
│   ├── schema.ts                 # Drizzle MySQL Schema
│   ├── relations.ts              # 表关系（当前为空）
│   └── seed.ts                   # 数据种子脚本
├── src/                          # 前端源码
│   ├── App.tsx                   # 路由配置
│   ├── main.tsx                  # React 入口
│   ├── components/               # 公共组件
│   │   ├── Layout.tsx            # 侧边栏 + 整体布局
│   │   └── ui/                   # shadcn/ui 组件（40+）
│   ├── hooks/useAuth.ts          # 认证 Hook
│   ├── pages/                    # 页面组件
│   │   ├── Login.tsx
│   │   ├── Dashboard.tsx
│   │   ├── Projects.tsx
│   │   ├── Analytics.tsx
│   │   ├── Users.tsx
│   │   └── Settings.tsx
│   └── providers/trpc.tsx        # tRPC + React Query 配置
├── drizzle.config.ts             # Drizzle Kit 配置（MySQL）
├── vite.config.ts                # Vite + Hono dev server 配置
├── package.json
└── tsconfig*.json
```

---

## 4. 数据库设计

数据库采用 MySQL，通过 `drizzle-orm/mysql-core` 定义。核心表如下：

### 4.1 `users` — 用户表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | serial PK | 自增主键 |
| username | varchar(100) unique | 用户名 |
| password | varchar(255) | bcrypt 哈希密码 |
| realName | varchar(100) | 真实姓名 |
| role | enum('admin','user','visitor') | 角色，默认 user |
| department | varchar(100) | 部门 |
| status | enum('active','disabled') | 状态 |
| lastLogin | timestamp | 最后登录时间 |
| createdAt | timestamp | 创建时间 |

### 4.2 `projects` — 项目表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | serial PK | 自增主键 |
| name | varchar(255) | 项目名称 |
| deadline | timestamp | 截止时间 |
| department | varchar(100) | 对接部门 |
| applyAmount | varchar(50) | 申报金额（文本存储，可为“荣誉”） |
| receiveAmount | varchar(50) | 到账金额 |
| status | enum('ok','failed','in_progress','pending') | 项目状态 |
| applicant | varchar(100) | 申报人 |
| applyDate | timestamp | 申报时间 |
| receiveDate | timestamp | 到账日期 |
| noticeUrl | varchar(500) | 申报通知网站 |
| publicUrl | varchar(500) | 公示链接 |
| notes | text | 备注 |
| warning | enum('normal','7days','15days','30days') | 预警级别 |
| createdAt / updatedAt | timestamp | 创建/更新时间 |

### 4.3 `settings` — 系统设置表

单表记录系统名称、时区、通知开关、安全与备份策略等配置。

---

## 5. 接口概览（tRPC Router）

| Router | Procedure | 类型 | 说明 |
|--------|-----------|------|------|
| auth | login | mutation | 用户名/密码登录，返回 JWT |
| auth | me | query | 获取当前登录用户 |
| project | list | query | 项目列表（分页/筛选/搜索） |
| project | getById | query | 单个项目 |
| project | create / update / delete | mutation | 项目 CRUD |
| project | stats | query | 项目统计卡片 |
| user | list | query | 用户列表 |
| user | create / update / delete | mutation | 用户 CRUD |
| setting | get | query | 读取系统设置 |
| setting | update | mutation | 更新系统设置 |
| analytics | overview | query | 分析概览 |
| analytics | monthlyTrend | query | 月度趋势 |
| analytics | departmentDistribution | query | 部门分布 |
| analytics | statusAnalysis | query | 状态分析 |
| analytics | applicantPerformance | query | 申报人绩效 |
| analytics | monthlyAmount | query | 月度金额 |
| analytics | departmentSuccess | query | 部门成功率 |

---

## 6. 认证与权限

- 登录成功后服务端使用 `jose` 签发 7 天有效期的 HS256 JWT。
- 前端将 Token 存入 `localStorage`，每次 tRPC 请求通过 `Authorization: Bearer <token>` 携带。
- tRPC 中间件定义了：
  - `publicQuery`：无需登录
  - `authedQuery`：需登录
  - `adminQuery`：需管理员角色
- **当前问题**：业务路由（project/user/setting/analytics）几乎全部使用 `publicQuery`，未真正区分读写权限，非管理员也能调用写接口。

---

## 7. 构建与部署

### 7.1 脚本

```json
{
  "dev": "vite",
  "build": "vite build && esbuild api/boot.ts --platform=node --bundle --format=esm --outdir=dist",
  "start": "NODE_ENV=production node dist/boot.js",
  "db:generate": "drizzle-kit generate",
  "db:migrate": "drizzle-kit migrate",
  "db:push": "drizzle-kit push"
}
```

### 7.2 生产启动流程

1. `vite build` 将前端资源打包到 `dist/public`
2. `esbuild` 将 `api/boot.ts` 打包到 `dist/boot.js`
3. `node dist/boot.js` 启动 Hono + Node 服务
4. Hono 挂载 tRPC `/api/trpc/*` 路由
5. Hono 通过 `@hono/node-server/serve-static` 托管 `dist/public`
6. 生产首次启动时执行 `seedDatabase()` 初始化数据

### 7.3 部署限制

- 当前架构强依赖 Node.js 运行时与 `@hono/node-server`，无法直接部署到 Vercel 的 Serverless/Edge 环境。
- MySQL 需要独立的数据库实例，与 Vercel 的原生 Serverless 数据库生态不兼容。
- 静态文件由 Node 服务托管，未能利用 Vercel 的 Edge Network 进行分发。
- 首次启动自动种子逻辑不适合无状态 Serverless 函数。

---

## 8. 关键依赖版本

| 依赖 | 版本 |
|------|------|
| react / react-dom | 19.2.0 |
| vite | 7.2.4 |
| hono | 4.8.3 |
| @trpc/server / client / react-query | 11.8.1 |
| drizzle-orm | 0.45.1 |
| drizzle-kit | 0.31.8 |
| mysql2 | 3.14.1 |
| @hono/node-server | 1.14.3 |
| recharts | 2.15.4 |
| tailwindcss | 3.4.19 |

---

## 9. 待改造点清单

1. 仪表盘展示数据需支持自定义增删改。
2. 项目管理字段需支持自定义增删改。
3. 数据分析卡片需支持编辑修改和删除。
4. 各模块数据需相互勾稽、联动。
5. 新增“企业荣誉库”模块，支持荣誉资质与复审周期管理。
6. 非管理员仅可查看，禁止修改/删除。
7. 适配 Vercel 部署，迁移数据库为 Vercel 友好方案（如 Vercel Postgres）。
8. 输出改造后技术文档。
